In the jschurch directory, run:

    bher -c biwa tests/simple.church

This generates `tests/simple.church.html`. 

Open this file in a browser to run the Church program in `tests/simple.church` via Javascript.
